// index.js:778 Uploader widget not found
const notifyUploadPlugin = {
    id: '@jupyterlab/filebrowser-extension:notify-upload'


webllm.ipynb - Seems we need to reload window after AnyWidget first initializes.


For PDF output like for slides:

```bash
apt update
apt-get install pandoc texlive-xetex texlive-fonts-recommended texlive-plain-generic
uv venv
source .venv/bin/activate
uv pip install --system jupyterlab-rise pandoc nbconvert
python -m nbconvert --to slides /workspaces/wiki3-ai-site/files/owners/slides.ipynb
```


```bash
juptrer server password
jupyter lab --port 8888
```